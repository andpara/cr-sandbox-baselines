- name: Initialize Sandbox Host
  hosts: debian-desktop
  gather_facts: yes
  become: yes
  roles:
    - name: user-access
      user_access_username: user
      user_access_password: Password123
      user_access_sudo: True
  tasks:
    - name: Print Msg
      debug:
        msg: "debian-12-sandbox-demo"